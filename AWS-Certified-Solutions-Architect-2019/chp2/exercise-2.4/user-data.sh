#!/bin/bash

## Description: 
# The user data starts the nginx server because the nginx server
# has already been installed in the custom AMI generate from existing EC2 instance.

sudo service nginx start